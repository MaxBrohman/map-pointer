import React from 'react';

import './map.sass';

const MapContainer = (): JSX.Element => {
    
    return (
        <div 
            className="map" 
            id="map" 
        ></div>
    );
};

export default MapContainer;